<div class="doctor-card">
    <div class="doctor-avatar <%= doctor.gender %>">
        <% if (doctor.photo) { %>
            <img src="<%= doctor.photo %>" alt="<%= doctor.name %>" loading="lazy">
        <% } else { %>
            <%= doctor.gender === 'male' ? '👨‍⚕️' : '👩‍⚕️' %>
        <% } %>
    </div>
    <div class="doctor-name"><%= doctor.name %></div>
    <div class="doctor-title"><%= doctor.title %></div>
    <p class="doctor-description">
        <%= doctor.shortDescription %>
    </p>
    <div class="doctor-specialties"><%= doctor.specialties %></div>
    <a href="doctor-detail.html?id=<%= doctor.id %>" class="appointment-btn">
        查看詳細 & 預約
    </a>
</div>